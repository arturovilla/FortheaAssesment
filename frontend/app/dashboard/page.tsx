// Dashboard home — placeholder for now. Charts (CPA/ROAS, anomalies) +
// macro context panels land here once the auth-aware API client is wired.

export default function DashboardPage() {
  return (
    <div className="text-sm text-ctp-subtext0">
      Dashboard shell is up. Charts coming next.
    </div>
  );
}
