# Schema management
alembic>=1.13
sqlalchemy>=2.0
psycopg[binary]>=3.2

# Config loading (.env)
python-dotenv>=1.0

# FastAPI app (added as endpoints come online)
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
httpx>=0.27

# Auth: verify Clerk JWTs (RS256 signature, JWKS-backed)
pyjwt[crypto]>=2.9

# CLI for helper scripts (e.g. scripts/get_test_token.py)
typer>=0.12
