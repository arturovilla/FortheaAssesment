# All Python deps for the shared venv. Used by:
#   - The FastAPI app (app/)
#   - Alembic migrations + helper scripts (alembic/, scripts/)
#   - The data generator + loader (data-generator/)
# Keep this file as the single source of truth — anything any of those needs
# goes here.

# ----- FastAPI app -----------------------------------------------------------
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
httpx>=0.27

# Auth: verify Clerk JWTs (RS256 signature, JWKS-backed)
pyjwt[crypto]>=2.9

# ----- Database (used by the app, alembic, and load.py) ----------------------
sqlalchemy>=2.0
psycopg[binary]>=3.2
alembic>=1.13

# ----- Config (.env files) ---------------------------------------------------
python-dotenv>=1.0

# ----- CLI framework (used by helper scripts, datagen.py, and load.py) -------
typer>=0.12
